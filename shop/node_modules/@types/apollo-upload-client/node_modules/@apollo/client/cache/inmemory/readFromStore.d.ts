import { SelectionSetNode } from 'apollo-upload-client/node_modules/graphql';
import { Reference, StoreObject } from 'apollo-upload-client/node_modules/@apollo/src/utilities';
import { Cache } from 'apollo-upload-client/node_modules/@apollo/src/cache/core/types/Cache';
import { DiffQueryAgainstStoreOptions, ReadMergeModifyContext } from 'apollo-upload-client/node_modules/@apollo/src/cache/inmemory/types';
import { InMemoryCache } from 'apollo-upload-client/node_modules/@apollo/src/cache/inmemory/inMemoryCache';
import { MissingTree } from 'apollo-upload-client/node_modules/@apollo/src/cache/core/types/common';
import { ObjectCanon } from 'apollo-upload-client/node_modules/@apollo/src/cache/inmemory/object-canon';
export declare type VariableMap = {
    [name: string]: any;
};
export declare type ExecResult<R = any> = {
    result: R;
    missing?: MissingTree;
};
export interface StoreReaderConfig {
    cache: InMemoryCache;
    addTypename?: boolean;
    resultCacheMaxSize?: number;
    canonizeResults?: boolean;
    canon?: ObjectCanon;
}
export declare class StoreReader {
    private executeSelectionSet;
    private executeSubSelectedArray;
    private config;
    private knownResults;
    canon: ObjectCanon;
    resetCanon(): void;
    constructor(config: StoreReaderConfig);
    diffQueryAgainstStore<T>({ store, query, rootId, variables, returnPartialData, canonizeResults, }: DiffQueryAgainstStoreOptions): Cache.DiffResult<T>;
    isFresh(result: Record<string, any>, parent: StoreObject | Reference, selectionSet: SelectionSetNode, context: ReadMergeModifyContext): boolean;
    private execSelectionSetImpl;
    private execSubSelectedArrayImpl;
}
//# sourceMappingURL=readFromStore.d.ts.map