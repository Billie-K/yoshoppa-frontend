import { DocumentNode, GraphQLError } from 'apollo-upload-client/node_modules/graphql';
import { Cache } from 'apollo-upload-client/node_modules/@apollo/src/cache';
import { WatchQueryOptions, ErrorPolicy } from 'apollo-upload-client/node_modules/@apollo/src/core/watchQueryOptions';
import { ObservableQuery } from 'apollo-upload-client/node_modules/@apollo/src/core/ObservableQuery';
import { QueryListener } from 'apollo-upload-client/node_modules/@apollo/src/core/types';
import { FetchResult } from 'apollo-upload-client/node_modules/@apollo/src/link/core';
import { ObservableSubscription } from 'apollo-upload-client/node_modules/@apollo/src/utilities';
import { NetworkStatus } from 'apollo-upload-client/node_modules/@apollo/src/core/networkStatus';
import { ApolloError } from 'apollo-upload-client/node_modules/@apollo/src/errors';
import { QueryManager } from 'apollo-upload-client/node_modules/@apollo/src/core/QueryManager';
export declare type QueryStoreValue = Pick<QueryInfo, "variables" | "networkStatus" | "networkError" | "graphQLErrors">;
export declare const enum CacheWriteBehavior {
    FORBID = 0,
    OVERWRITE = 1,
    MERGE = 2
}
export declare class QueryInfo {
    readonly queryId: string;
    listeners: Set<QueryListener>;
    document: DocumentNode | null;
    lastRequestId: number;
    subscriptions: Set<ObservableSubscription>;
    variables?: Record<string, any>;
    networkStatus?: NetworkStatus;
    networkError?: Error | null;
    graphQLErrors?: ReadonlyArray<GraphQLError>;
    stopped: boolean;
    private cache;
    constructor(queryManager: QueryManager<any>, queryId?: string);
    init(query: {
        document: DocumentNode;
        variables: Record<string, any> | undefined;
        networkStatus?: NetworkStatus;
        observableQuery?: ObservableQuery<any>;
        lastRequestId?: number;
    }): this;
    private dirty;
    private notifyTimeout?;
    reset(): void;
    getDiff(variables?: Record<string, any> | undefined): Cache.DiffResult<any>;
    private lastDiff?;
    private updateLastDiff;
    private getDiffOptions;
    setDiff(diff: Cache.DiffResult<any> | null): void;
    readonly observableQuery: ObservableQuery<any> | null;
    private oqListener?;
    setObservableQuery(oq: ObservableQuery<any> | null): void;
    notify(): void;
    private shouldNotify;
    stop(): void;
    private cancel;
    private lastWatch?;
    private updateWatch;
    private lastWrite?;
    resetLastWrite(): void;
    private shouldWrite;
    markResult<T>(result: FetchResult<T>, options: Pick<WatchQueryOptions, "variables" | "fetchPolicy" | "errorPolicy">, cacheWriteBehavior: CacheWriteBehavior): void;
    markReady(): NetworkStatus;
    markError(error: ApolloError): ApolloError;
}
export declare function shouldWriteResult<T>(result: FetchResult<T>, errorPolicy?: ErrorPolicy): boolean;
//# sourceMappingURL=QueryInfo.d.ts.map