import type { ASTNode } from 'apollo-upload-client/node_modules/graphql/language/ast';
/**
 * Converts an AST into a string, using one set of reasonable
 * formatting rules.
 */
export declare function print(ast: ASTNode): string;
