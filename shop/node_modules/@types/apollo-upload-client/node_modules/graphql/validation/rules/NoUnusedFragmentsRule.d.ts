import type { ASTVisitor } from 'apollo-upload-client/node_modules/graphql/language/visitor';
import type { ASTValidationContext } from 'apollo-upload-client/node_modules/graphql/validation/ValidationContext';
/**
 * No unused fragments
 *
 * A GraphQL document is only valid if all fragment definitions are spread
 * within operations, or spread within other fragments spread within operations.
 *
 * See https://spec.graphql.org/draft/#sec-Fragments-Must-Be-Used
 */
export declare function NoUnusedFragmentsRule(
  context: ASTValidationContext,
): ASTVisitor;
