import type { ASTVisitor } from 'apollo-upload-client/node_modules/graphql/language/visitor';
import type { SDLValidationContext } from 'apollo-upload-client/node_modules/graphql/validation/ValidationContext';
/**
 * Possible type extension
 *
 * A type extension is only valid if the type is defined and has the same kind.
 */
export declare function PossibleTypeExtensionsRule(
  context: SDLValidationContext,
): ASTVisitor;
