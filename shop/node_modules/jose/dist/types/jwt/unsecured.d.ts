import type { JWSHeaderParameters, JWTClaimVerificationOptions, JWTPayload } from 'jose/dist/types/types';
import { ProduceJWT } from 'jose/dist/types/jwt/produce';
export interface UnsecuredResult {
    payload: JWTPayload;
    header: JWSHeaderParameters;
}
export declare class UnsecuredJWT extends ProduceJWT {
    encode(): string;
    static decode(jwt: string, options?: JWTClaimVerificationOptions): UnsecuredResult;
}
